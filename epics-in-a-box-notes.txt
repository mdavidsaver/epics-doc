
EPICS in a box

0) Prep

0.1)Collect sources

Base
extensions config(ure)
edm
alh
striptool
an archiver?
aynApps
  autosave/restore

0.2) Organization

mydir/base/...
     /extensions/...

0.3)

Environment

EPICS_HOST_ARCH

EPICS_BASE
EPICS_EXTENSIONS

1) Building

1.1) Base

$ cd mydir
$ tar -xzf BaseR3.14.10.tar.gz
$ ln -s base-3.14.10 base
$ export EPICS_BASE=$PWD/base
$ cd base
$ sh startup/EpicsHostArch
linux-x86
$ export EPICS_HOST_ARCH=`sh startup/EpicsHostArch`
$ make

1.2) EDM

